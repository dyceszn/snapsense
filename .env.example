# Get a free Groq API key (no credit card) at https://console.groq.com/keys
# Note: no VITE_ prefix — the key is injected by the Vite proxy and never bundled
GROQ_API_KEY=your_groq_api_key_here

# Optional overrides
VITE_MAX_UPLOAD_BYTES=8388608
